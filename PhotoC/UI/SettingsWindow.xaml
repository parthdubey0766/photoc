<Window x:Class="PhotoC.UI.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="Transparent"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="pack://application:,,,/Assets/PhotoC.ico"
        Title="PhotoC Settings" Height="760" Width="680"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Border CornerRadius="18" Background="#0D1117" BorderBrush="#30363D" BorderThickness="1" Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="28" ShadowDepth="4" Opacity="0.35" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" Margin="0,16,0,8" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <Border Grid.Column="0" Width="42" Height="42" CornerRadius="12" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" Margin="18,0,12,0">
                    <TextBlock Text="📷" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0">
                    <TextBlock Text="PhotoC" FontSize="24" FontWeight="SemiBold" Foreground="#F0F6FC"/>
                    <TextBlock Text="Phone Photo Auto-Compressor" FontSize="12" Foreground="#8B949E"/>
                    <TextBlock Text="Created by Parth Dubey" FontSize="11" Foreground="#58A6FF" Margin="0,2,0,0"/>
                </StackPanel>

                <Button Grid.Column="2" x:Name="CloseButton" Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Margin="0,0,12,0" Padding="0" Click="CloseButton_Click">
                    <md:PackIcon Kind="Close" Width="18" Height="18" Foreground="#8B949E" />
                </Button>
            </Grid>

            <!-- Content Area -->
            <ScrollViewer Grid.Row="1" Margin="10,10,10,10" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="12" Width="Auto">

                    <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="14" Padding="18" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="Settings overview" FontSize="18" FontWeight="SemiBold" Foreground="#F0F6FC"/>
                            <TextBlock Text="Choose a folder, tune compression, and keep the app running quietly in the tray." Foreground="#8B949E" Margin="0,6,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- Watched Folder Card -->
                    <md:Card Padding="18" Margin="0,0,0,16" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" UniformCornerRadius="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <md:PackIcon Kind="FolderOpenOutline" VerticalAlignment="Center" Foreground="#58A6FF" Margin="0,0,8,0"/>
                                <TextBlock Text="Watched folder" FontWeight="SemiBold" Foreground="#F0F6FC" FontSize="16"/>
                            </StackPanel>
                            <TextBlock Text="PhotoC watches this folder for new images. Point it to your Phone Link, iCloud, or other sync location." Foreground="#8B949E" TextWrapping="Wrap" Margin="0,0,0,12"/>
                            
                            <Grid Margin="0,4,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox x:Name="FolderPathBox" Grid.Column="0" IsReadOnly="True" 
                                         md:HintAssist.Hint="Select your Phone Link sync folder..." 
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0" Height="48"/>
                                <Button Grid.Column="1" Content="Browse..." Click="BrowseButton_Click" Height="48" Padding="18,0" />
                            </Grid>
                        </StackPanel>
                    </md:Card>

                    <!-- Output Folder Card -->
                    <md:Card Padding="18" Margin="0,0,0,16" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" UniformCornerRadius="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <md:PackIcon Kind="FolderMoveOutline" VerticalAlignment="Center" Foreground="#3FB950" Margin="0,0,8,0"/>
                                <TextBlock Text="Output folder" FontWeight="SemiBold" Foreground="#F0F6FC" FontSize="16"/>
                            </StackPanel>
                            <TextBlock Text="Save compressed photos to a separate folder, keeping your originals untouched. If disabled, originals are replaced in-place." Foreground="#8B949E" TextWrapping="Wrap" Margin="0,0,0,12"/>

                            <CheckBox x:Name="SaveToOutputFolderCheckBox" Content="Save compressed photos to a separate folder" Margin="0,0,0,12" Foreground="#F0F6FC" Checked="OutputFolderToggle_Changed" Unchecked="OutputFolderToggle_Changed"/>

                            <Grid x:Name="OutputFolderGrid" Margin="0,4,0,0" Visibility="Collapsed">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <TextBox x:Name="OutputFolderPathBox" Grid.Column="0" IsReadOnly="True" 
                                         md:HintAssist.Hint="Select output folder for compressed photos..." 
                                         Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,0,10,0" Height="48"/>
                                <Button Grid.Column="1" Content="Browse..." Click="BrowseOutputButton_Click" Height="48" Padding="18,0" />
                            </Grid>
                            <TextBlock x:Name="OutputFolderHint" Text="Originals will be replaced in-place." FontSize="11" Foreground="#8B949E" Margin="0,6,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </md:Card>

                    <!-- Quality Card -->
                    <md:Card Padding="18" Margin="0,0,0,16" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" UniformCornerRadius="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <md:PackIcon Kind="LightningBoltOutline" VerticalAlignment="Center" Foreground="#58A6FF" Margin="0,0,8,0"/>
                                <TextBlock Text="Compression quality" FontWeight="SemiBold" Foreground="#F0F6FC" FontSize="16"/>
                            </StackPanel>
                            <TextBlock Text="ShortPixel SmartCompress™-style adaptive compression: PhotoC tries multiple quality levels (Q80 → Q70) and keeps the smallest visually lossless result. Expect 60–95% reduction on phone photos." Foreground="#8B949E" TextWrapping="Wrap" Margin="0,0,0,12"/>

                            <!-- JPEG -->
                            <Grid Margin="0,10,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="170" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="JPEG Quality" VerticalAlignment="Center" Foreground="#F0F6FC"/>
                                <Slider Grid.Column="1" x:Name="JpegSlider" Minimum="60" Maximum="100" Value="80" TickFrequency="1" AutoToolTipPlacement="TopLeft" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True" VerticalAlignment="Center" Margin="10,0" Height="28"/>
                                <TextBlock Grid.Column="2" Text="{Binding ElementName=JpegSlider, Path=Value}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#58A6FF"/>
                            </Grid>
                            <TextBlock Text="Q80 = ShortPixel Glossy (visually lossless). Q70 = ShortPixel Lossy (max savings). Smart sweep tests all levels in between and keeps the smallest." FontSize="11" Foreground="#8B949E" Margin="170,0,0,10" TextWrapping="Wrap"/>

                            <!-- WebP -->
                            <Grid Margin="0,8,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="170" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="50" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="WebP Quality" VerticalAlignment="Center" Foreground="#F0F6FC"/>
                                <Slider Grid.Column="1" x:Name="WebpSlider" Minimum="1" Maximum="100" Value="100" TickFrequency="1" AutoToolTipPlacement="TopLeft" IsSnapToTickEnabled="True" IsMoveToPointEnabled="True" VerticalAlignment="Center" Margin="10,0" Height="28"/>
                                <TextBlock Grid.Column="2" Text="{Binding ElementName=WebpSlider, Path=Value}" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#58A6FF"/>
                            </Grid>
                            <TextBlock Text="WebP is set to lossless mode for exact pixel preservation." FontSize="11" Foreground="#8B949E" Margin="170,0,0,10" TextWrapping="Wrap"/>

                            <!-- PNG -->
                            <Grid Margin="0,10,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="PNG Level (0-9)" VerticalAlignment="Center" Foreground="#F0F6FC"/>
                                <TextBox Grid.Column="1" x:Name="PngLevelBox" Text="9" MaxLength="1" Width="72" HorizontalAlignment="Left" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="44" TextAlignment="Center" VerticalContentAlignment="Center" Padding="0" FontSize="16"/>
                            </Grid>
                            <TextBlock Text="Level 9 gives the strongest lossless compression for PNG, BMP-to-PNG, and TIFF-to-PNG." FontSize="11" Foreground="#8B949E" Margin="200,5,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </md:Card>

                    <!-- Behavior Card -->
                    <md:Card Padding="18" Margin="0,0,0,16" Background="#161B22" BorderBrush="#30363D" BorderThickness="1" UniformCornerRadius="14">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                <md:PackIcon Kind="CogOutline" VerticalAlignment="Center" Foreground="#58A6FF" Margin="0,0,8,0"/>
                                <TextBlock Text="Behavior" FontWeight="SemiBold" Foreground="#F0F6FC" FontSize="16"/>
                            </StackPanel>
                            <TextBlock Text="These controls decide when files are skipped and whether PhotoC starts with Windows." Foreground="#8B949E" TextWrapping="Wrap" Margin="0,0,0,12"/>

                            <Grid Margin="0,5,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="210" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Max image dimension (px)" VerticalAlignment="Center" Foreground="#F0F6FC"/>
                                <TextBox Grid.Column="1" x:Name="MaxDimensionBox" Text="0" Width="110" HorizontalAlignment="Left" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="44" TextAlignment="Center" VerticalContentAlignment="Center" Padding="0" FontSize="16"/>
                            </Grid>
                            <TextBlock Text="Downscale oversized photos before compression. 0 = disabled (no quality loss). Set to 3840 for 4K if you want smaller files at the cost of resolution." FontSize="11" Foreground="#8B949E" Margin="0,0,0,12" TextWrapping="Wrap"/>

                            <Grid Margin="0,5,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="210" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Skip files under (KB)" VerticalAlignment="Center" Foreground="#F0F6FC"/>
                                <TextBox Grid.Column="1" x:Name="MinSizeBox" Text="2048" Width="110" HorizontalAlignment="Left" Style="{StaticResource MaterialDesignOutlinedTextBox}" Height="44" TextAlignment="Center" VerticalContentAlignment="Center" Padding="0" FontSize="16"/>
                            </Grid>
                            <TextBlock Text="Smaller files usually do not benefit much from recompression, so they are skipped to save CPU." FontSize="11" Foreground="#8B949E" Margin="0,0,0,12" TextWrapping="Wrap"/>

                            <CheckBox x:Name="StartupCheckBox" Content="Run PhotoC automatically when Windows starts" Margin="0,0,0,5" Foreground="#F0F6FC"/>
                            <TextBlock Text="Adds or removes a registry entry under the current user only." FontSize="11" Foreground="#8B949E" Margin="28,2,0,0" TextWrapping="Wrap"/>
                        </StackPanel>
                    </md:Card>

                    <md:DialogHost x:Name="FlashDialogHost">
                        <md:DialogHost.DialogContent>
                            <StackPanel Margin="20" Orientation="Horizontal">
                                <md:PackIcon Kind="CheckCircle" Foreground="#3FB950" Width="24" Height="24" VerticalAlignment="Center"/>
                                <TextBlock Text="Settings Saved and Watcher Restarted." Margin="10,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </md:DialogHost.DialogContent>
                    </md:DialogHost>

                    <!-- Status Text -->
                    <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="12" Padding="14" Margin="0,0,0,0">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <md:PackIcon x:Name="StatusDot" Kind="Circle" Foreground="Red" VerticalAlignment="Center" Margin="0,0,8,0" Width="10" Height="10"/>
                            <TextBlock x:Name="StatusText" Text="Loading status..." Opacity="0.9" FontSize="12" Foreground="#C9D1D9"/>
                        </StackPanel>
                    </Border>
                    <Border Background="#161B22" BorderBrush="#30363D" BorderThickness="1" CornerRadius="12" Padding="14" Margin="0,14,0,0">
                        <StackPanel>
                            <TextBlock Text="What each option does" FontSize="14" FontWeight="SemiBold" Foreground="#F0F6FC"/>
                            <TextBlock Text="JPEG controls re-encode quality for camera photos. PNG level controls exact lossless compression. The skip threshold avoids wasting CPU on small files. Startup controls whether PhotoC appears automatically after login." Foreground="#8B949E" TextWrapping="Wrap" Margin="0,6,0,0"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="Created by Parth Dubey" Foreground="#58A6FF" FontSize="11" HorizontalAlignment="Right" Margin="0,10,4,0"/>
                </StackPanel>
            </ScrollViewer>

            <!-- Buttons Grid -->
            <Grid Grid.Row="2" Margin="30,10,30,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <Button Grid.Column="0" Content="Save &amp; Apply" Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,15,0" Click="SaveButton_Click"/>
                <Button Grid.Column="1" Content="View Log" Style="{StaticResource MaterialDesignOutlinedButton}" Margin="0,0,15,0" Click="ViewLogButton_Click"/>
                <Button Grid.Column="2" Content="Cancel" Style="{StaticResource MaterialDesignFlatButton}" Click="CloseButton_Click"/>
            </Grid>

        </Grid>
    </Border>
</Window>
