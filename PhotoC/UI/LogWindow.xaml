<Window x:Class="PhotoC.UI.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="Transparent"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="pack://application:,,,/Assets/PhotoC.ico"
        Title="PhotoC Logs" Height="560" Width="850"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <Storyboard x:Key="WindowFadeIn">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0" To="1" Duration="0:0:0.35"/>
        </Storyboard>
    </Window.Resources>

    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard Storyboard="{StaticResource WindowFadeIn}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border CornerRadius="18" Margin="10">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0D1117" Offset="0"/>
                <GradientStop Color="#111821" Offset="0.5"/>
                <GradientStop Color="#0D1117" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#30363D" Offset="0"/>
                <GradientStop Color="#21262D" Offset="0.5"/>
                <GradientStop Color="#30363D" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.BorderThickness>
            <Thickness>1</Thickness>
        </Border.BorderThickness>
        <Border.Effect>
            <DropShadowEffect BlurRadius="32" ShadowDepth="4" Opacity="0.4" Color="#000000" />
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Grid Grid.Row="0" Margin="0,14,0,4" Background="Transparent">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" Margin="20,0,0,0" VerticalAlignment="Center">
                    <Border Width="30" Height="30" CornerRadius="8" Margin="0,0,10,0">
                        <Border.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                <GradientStop Color="#1A2332" Offset="0"/>
                                <GradientStop Color="#161B22" Offset="1"/>
                            </LinearGradientBrush>
                        </Border.Background>
                        <md:PackIcon Kind="TextBoxOutline" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#58A6FF"/>
                    </Border>
                    <TextBlock Text="PhotoC Logs" FontSize="18" FontWeight="SemiBold" Foreground="#F0F6FC" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Grid.Column="2" Style="{StaticResource MaterialDesignFlatButton}" Width="40" Height="40" Margin="0,0,12,0" Padding="0" Click="CloseButton_Click">
                    <md:PackIcon Kind="Close" Width="16" Height="16" Foreground="#6E7681" />
                </Button>
            </Grid>

            <!-- Log output -->
            <Border Grid.Row="1" Margin="16,8,16,8" CornerRadius="12" Background="#010409" BorderBrush="#21262D" BorderThickness="1">
                <RichTextBox x:Name="LogBox" IsReadOnly="True" FontFamily="Cascadia Code, Consolas, monospace" FontSize="12" VerticalScrollBarVisibility="Auto" Background="Transparent" Foreground="#C9D1D9" BorderThickness="0" Padding="14"/>
            </Border>

            <!-- Button bar -->
            <Border Grid.Row="2" Padding="16,8,16,14">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <!-- Clear View button removed -->
                    <Button Grid.Column="1" Content="Open Log Folder" Click="OpenFolderButton_Click" Margin="0,0,0,0" Height="38">
                        <Button.Background>
                            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                <GradientStop Color="#238636" Offset="0"/>
                                <GradientStop Color="#2EA043" Offset="1"/>
                            </LinearGradientBrush>
                        </Button.Background>
                    </Button>
                </Grid>
            </Border>

        </Grid>
    </Border>
</Window>
