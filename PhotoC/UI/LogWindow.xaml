<Window x:Class="PhotoC.UI.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Icon="pack://application:,,,/Assets/PhotoC.ico"
        Title="PhotoC Logs" Height="500" Width="800"
        WindowStartupLocation="CenterScreen">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <RichTextBox x:Name="LogBox" Grid.Row="0" IsReadOnly="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" Margin="10" Background="Black" Foreground="White" BorderThickness="0" Padding="10"/>

        <Grid Grid.Row="1" Margin="10,0,10,10" Background="{DynamicResource MaterialDesignCardBackground}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Clear View" Style="{StaticResource MaterialDesignOutlinedButton}" Click="ClearButton_Click" Margin="0,10,10,10"/>
            <Button Grid.Column="1" Content="Open Log Folder" Style="{StaticResource MaterialDesignRaisedButton}" Click="OpenFolderButton_Click" Margin="0,10,0,10"/>
        </Grid>
    </Grid>
</Window>
